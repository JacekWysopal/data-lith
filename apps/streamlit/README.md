# Streamlit Dashboards

Streamlit app for exploring curated analytics tables from DuckDB.

## Run

From the repository root:

```powershell
docker compose --profile dashboards up -d streamlit
```

Open <http://localhost:8501>.

